# 管理者サイト仕様

## 全体レイアウト

`app/(dashboard)/` にルートグループを切り、その `layout.tsx` で左サイドメニュー＋本文エリアの枠を作る（URLは変わらない）。
ログイン画面（#8）はこのグループの外に置くので、サイドメニューは出ない。
`/articles`（記事一覧）とその配下もメニューには出さない。

PC（1440×900）前提。スマホ・タブレット幅への対応はしない。

### サイドメニュー

- 項目の定義は配列1か所にまとめてあり、ページが増えたらそこに足すだけでよい
- いま開いているページの項目をハイライトする（背景 `sky-100` + アイコン・文字を `sky-500` で太字化）
- 未実装ページの項目は無効表示（クリック不可・グレーアウト）にし、「準備中」バッジを付ける。404には飛ばさない
- 開閉できる。閉じるとアイコンだけの細い表示になる。開閉状態は Zustand（`stores/ui.ts` の `isSidebarOpen`）に持ち、永続化しない（リロードで開いた状態に戻る）

#### メニュー項目

| 表示名         | パス           | 現状   |
| -------------- | -------------- | ------ |
| ダッシュボード | `/`            | 実装済 |
| 基本設定       | `/settings`    | 準備中 |
| 機能オン・オフ | `/features`    | 準備中 |
| ホーム         | `/home`        | 準備中 |
| ニュース       | `/news`        | 準備中 |
| ブログ         | `/blogs`       | 準備中 |
| スケジュール   | `/schedule`    | 準備中 |
| マップ         | `/map`         | 準備中 |
| 天気           | `/weather`     | 準備中 |
| 模擬店         | `/shops`       | 準備中 |
| 出演者         | `/artists`     | 準備中 |
| 権限設定       | `/permissions` | 準備中 |

## ページ

### ダッシュボード（`/`）

見出しだけの空ページ。中身は未着手。

### 記事一覧（`/articles`）

- 対象のイベントは env の `NEXT_PUBLIC_EVENT_ID` で固定する
- そのイベントの記事を、更新日時の新しい順に先頭100件まで並べる（ページングは付けない）
- 各行にタイトルと更新日時（`YYYY/MM/DD HH:mm`）を出し、タイトルのクリックで `/articles/[id]` へ移動する
- タイトルが空の記事は「（無題）」と出す
- 0件のときは「記事がありません」と出す
- 「新規作成」ボタンでタイトル・本文とも空の記事を作り（`POST /api/articles`）、作った記事の `/articles/[id]` へ移動する

### 記事エディタ（`/articles/[id]`）

BlockNote（[`docs/article-system.md`](./article-system.md)）でテキスト・見出し・リスト・
チェックリスト・トグルリスト・引用・区切り線・表を編集する画面。

- 開いたときに `GET /api/articles/:id` で記事を読み込み、タイトルの入力欄とエディタの初期値にする
- 記事が見つからない（404）ときは、エディタの代わりに「記事が見つかりません」と一覧へのリンクを出す
- 本文の上にタイトルの入力欄を置く（100文字まで。空でもよい）
- 「保存」ボタンを押したときだけ、タイトルと本文をまとめて保存する（`PUT /api/articles/:id`）。自動保存はしない
    - タイトルが100文字を超えていたら、入力欄の下にエラーを出して保存しない
    - 保存中はボタンを無効にする
    - 成功したら「保存しました」、失敗したらエラーメッセージを出す
- 本文のJSONを確認用の折りたたみで出す
